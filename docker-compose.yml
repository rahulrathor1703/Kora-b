services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: markos
      POSTGRES_PASSWORD: markos
      POSTGRES_DB: markos
    ports:
      - '5435:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
